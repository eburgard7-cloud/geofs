test/node_modules/
**/__pycache__/
# Local-uvicorn-run fallback for the tile proxy / OG image disk cache (see app.py's
# _default_tile_cache_dir()) -- production caches under a tiles/ dir next to RACE_DB.
.tile_cache/
og/
