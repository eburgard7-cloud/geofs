# Docs overhaul report

Working notes and hand-off for the docs overhaul (phases D0–D6). Baseline: `main` at `844b229`
(client `CONFIG.VERSION` `1.7.0`, server `SERVER_VERSION` `1.5.0`, relay `PROTO` 8), 2026-09-24.

## Branch note

The session prompt names the branch `docs-overhaul`. This cloud session is only authorized to push
to its designated branch, `claude/docs-overhaul-9p0xhq`, so every commit went there instead. To use
the requested name, rename or re-push it on GitHub (`git push origin
claude/docs-overhaul-9p0xhq:docs-overhaul`). Nothing was merged. `main` and `deploy` were not
touched.

## D0 — inventory

### Every Markdown file, and what happens to it

| File | Lines | Decision |
|---|---|---|
| `README.md` | 309 | **Rewrite** (D3). Player-first showpiece. The CONFIG table moves to the generated `docs/REFERENCE.md`, hosting moves to `docs/RUNBOOK.md`, and the architecture and layout sections are refreshed. |
| `CLAUDE.md` | 33 | **Keep.** Only broken links or paths may change (none found, see below). |
| `race/README.md` | 1423 | **Slim to a module guide** (D5, ≤ 300 lines). Operational content moves to `docs/RUNBOOK.md`, reference content moves to `docs/REFERENCE.md`, and mechanics are condensed. The porting map is in D6. |
| `race/PROTOCOL.md` | 1346 | **Keep, format only** (D5): TOC, a frame index table, consistent heading levels. No fact changes. |
| `race/ACCEPTANCE.md` | 476 | **Merge target** (D5). Absorbs `race/docs/ACCEPTANCE.md`. |
| `race/docs/ACCEPTANCE.md` | 125 | **Merge into `race/ACCEPTANCE.md`, then stub** as a one-line pointer. |
| `race/docs/AUDIT.md` | 275 | **Keep as history** under a dated "Historical audits" heading (D5). It is a 2026-09-23 snapshot at 1.3.1 and several findings are still open. |
| `race/CHANGELOG.md` | 146 | **Normalize** (D5) to Keep-a-Changelog headings, newest first. All history is kept. |
| `race/server/DEPLOY_CHECKLIST.md` | 595 | **Merge into `docs/RUNBOOK.md`, then stub** (D4). The stub is 3 lines so old links still resolve. |
| `race/courses/CUPS.md` | 30 | **Off-limits tonight.** Linked, not edited. |
| *(new)* `docs/README.md` | | Docs index (D5). |
| *(new)* `docs/RUNBOOK.md` | | The single operational source of truth (D4). |
| *(new)* `docs/REFERENCE.md` | | Generated by `race/tools/gen_docs.py` (D1). |
| *(new)* `docs/assets/banner.svg`, `divider.svg` | | Brand assets (D2). |

Tonight's off-limits files, which are linked only: `race/courses/CUPS.md`, `race/runways/*`,
`race/ADDONS.md`, `race/docs/LAPS.md`, `race/docs/BUSH_MODE.md`, `OVERNIGHT_REPORT.md`. Of these,
`race/ADDONS.md`, `race/docs/LAPS.md`, `race/docs/BUSH_MODE.md` and `OVERNIGHT_REPORT.md` don't
exist at the baseline. Any link to them is deliberate and listed under "Links to tonight's files".

### Duplicates found

- **Two acceptance checklists.** `race/ACCEPTANCE.md` is the race-night script. Its Parts 0–1
  predate the 1.3.0 shell. `race/docs/ACCEPTANCE.md` is the lobby checklist. Its §6 points back
  into the first file, and they overlap on preflight, ready, force-start, abort, host handoff and
  spectating.
- **Key maps in three places:** root `README.md:65-81`, `race/README.md:81-101`, plus the in-panel
  `kbd` hints in `race.js`. They disagree on Alt+H (`race/README.md` said "panel").
- **The CONFIG table** exists only in the root README (`README.md:193-305`), is hand-maintained,
  and the README itself says nothing checks it.
- **Test commands** appear in three places: `README.md:175-181`, `race/README.md:1143-1149` and
  `CLAUDE.md`.
- **Deploy steps** are in three places: root `README.md:160-169`, `race/README.md:930-956`
  ("Leaderboard server (homelab)", whose heading is duplicated on lines 930–931), and
  `DEPLOY_CHECKLIST.md`, which is the most complete.
- **The endpoint table** is in `race/README.md:795-800` and `958-977` (partly repeated) and in
  PROTOCOL.md "Proto 6 → REST".
- **The item table** is in the root README (`55-61`) and `race/README.md:518-524`.
- **The frame index** is in `race/README.md:1020-1027`, which stops at proto 4, and in PROTOCOL.md.
- **Terrain status** is in three places: `race/README.md:276-306` (stale), `CUPS.md` (current),
  and `KNOWN_TERRAIN_STATUS` in `race.js:6137` (stale, code, not touched).

### Stale or contradictory claims (file:line → what the code says)

| # | Where | Claim | Reality (verified) |
|---|---|---|---|
| S1 | `README.md:3` | CI badge | Kept out of the new README (no badges, per brief). CI does exist at `.github/workflows/test.yml`. |
| S2 | `README.md:204` | `VERSION` default `'1.0.0'` | `race.js:12` `VERSION: '1.7.0'` |
| S3 | `README.md:207` | `API_BASE` default `''` | `race.js:20` `API_BASE: 'https://race.finsonly.net'` (set since 1.3.1) |
| S4 | `README.md:257` | `POWERUP_BOOST_ADD_MS` `35` | `race.js:65` `50` (m/s, ramped over `BOOST_RAMP_MS` 1000 in `BOOST_RAMP_STEPS` 10, capped by `BOOST_MAX_KT` 650) |
| S5 | `README.md:300-303` | `SAFE_WRITES`, `VELOCITY_FRAME`, `BOOST_LLA_FALLBACK` rows | Removed in 1.7.0 (CHANGELOG 1.7.0; not in `race.js` CONFIG) |
| S6 | `README.md:193-305` | CONFIG table "every key" | Missing `LOBBY_V2`, `SHELL_AUTO_COLLAPSE`, `CHAT_ENABLED`, `RAMP_PRESENCE_HZ`, `RAMP_PING_DAILY_CAP`, `BOOST_MAX_KT`, `BOOST_RAMP_MS`, `BOOST_RAMP_STEPS`, `PACE_KT`, `ROLLING_START`, `FORMATION_*` (12 keys), `START_LINE_SETBACK_M`, `OVAL_*`, and more. Replaced by the generated table. |
| S7 | `README.md:57` | Boost "about 4 s of extra speed" | The effect lasts `POWERUP_BOOST_MS` 4000. The speed itself is +50 m/s over 1 s, capped at 650 kt. Both are true, so the new README says both. |
| S8 | `README.md:59` | Banana "about 80 m wide" | Client trip **radius** `BANANA_RADIUS_M: 80` (`race.js:84`). The server radius is `BANANA_RADIUS_M = 75.0` (`app.py:1333`) plus `TRIP_SLACK_M` 400. |
| S9 | `README.md:114-123` | Four courses. Gorge and Crater "Not flyable yet". | 15 courses in `race/courses/index.json`. `CUPS.md` (2026-09-22) says gorge-run and crater-rim were repaired to v2 and are flyable. |
| S10 | `README.md:88` | Smoke test "in race/server/DEPLOY_CHECKLIST.md" | Moves to `docs/RUNBOOK.md` (the stub keeps the link alive). |
| S11 | `README.md:157` | "the four pytest suites" | There are four `test_*.py` files, but only `test_server.py` is in CI and in CLAUDE.md's required list. |
| S12 | `README.md:173,176` / `race/README.md:1144` | `npm i jsdom@24` | `race/test/package.json` pins `jsdom ^24.0.0`, so `npm install` (CLAUDE.md) or `npm ci` (CI) is enough. Both work. |
| S13 | `README.md:185-191` | Release = tag `race-vX.Y.Z` | Tags `race-v0.5.0`, `race-v1.0.0` and `race-v1.2.0` exist on origin. Deploys now go through the `deploy` branch (`autodeploy.sh`). The FALLBACK pin is still `race-v1.0.0` (`bookmarklet.txt`), so fallback users get 1.0.0. |
| S14 | `README.md:309` | "No LICENSE file yet … all-rights-reserved" | Still true: no LICENSE file. The new README keeps "All rights reserved". Needs Eric's decision (see "Decisions"). |
| S15 | `README.md:36-45`, `race/README.md:578-615` | "Lobby" describes the old floating card, the confirm() force start and "no free-text chat" | Under `LOBBY_V2: true` (default) the card is never built (`LegacyUI`, CHANGELOG ui-unify). Free-text chat is proto 5. Force start is the Gate's **Start anyway**. |
| S16 | `race/README.md:35` | "PROTOCOL.md … proto 1–6" | PROTOCOL.md documents protos 1–8. `app.py:1318` `PROTO = 8`. |
| S17 | `race/README.md:282` | "All four courses carry item boxes" | 15 courses. Not re-verified per course. The new doc no longer makes this claim. |
| S18 | `race/README.md:287-301` | gorge-run and crater-rim **FAIL** terrain | Repaired to v2 per `CUPS.md`. `KNOWN_TERRAIN_STATUS` in `race.js:6137` still says `'fail'` (code, not touched; AUDIT B4 still open). |
| S19 | `race/README.md:930-931` | Heading duplicated | Deduplicated on the move to the runbook. |
| S20 | `race/README.md:937-947` | Deploy by `cp` to `stack/race-api` then `docker restart caddy` | Contradicts CLAUDE.md ("never restart containers without explicit approval") and the checklist (`caddy reload` after `validate`). The runbook keeps only the checklist's path. |
| S21 | `race/README.md:1349-1352` | "The 1.2.0 hub is server-side only … no shipped client talks to them" | 1.3.0+ clients use the hub (`LOBBY_V2`, the Ramp). |
| S22 | `race/README.md:1368-1371` | "Boost writes probe-confirmed speed scalars … velocity vector half stays off" | 1.7.0 moved Boost onto `GeoPhysics.addSpeedAlongPath` via `rigidBody.setLinearVelocity` (`race/README.md:876-899` says so itself). |
| S23 | `race/PROTOCOL.md:7-9` | "The relay carries five things" then lists six (protos 1–6) | Protos 7 and 8 were added later. This is a wording issue in a wire spec, so the text is left as is (format-only rule). |
| S24 | `race/PROTOCOL.md:16`, `:202` | "`PROTO` (currently **6**)" / example `"proto": 6` | `app.py:1318` `PROTO = 8`. The proto 7 and 8 sections say `joined` now carries 7 and 8. Left in place (format-only rule) and flagged here. |
| S25 | `race/PROTOCOL.md:397` | "see 'Manual sync' below" | There is no such section (AUDIT R8). |
| S26 | `race/PROTOCOL.md:1257` | "See race/formation.js's pure geometry" | There is no `race/formation.js`. Formation is inside `race.js` (`formation*` functions, the `_internals` export at `race.js:9203-9205`). The `race.js:166` comment has the same stale path (code, not touched). |
| S27 | `race/PROTOCOL.md:829-830`, `:850`, `:1186` | Points to `server/DEPLOY_CHECKLIST.md` §7 | Still resolves through the stub. The canonical place is now `docs/RUNBOOK.md`. |
| S28 | `race/server/DEPLOY_CHECKLIST.md:7` | "Per `race/CLAUDE.md`" | The file is `CLAUDE.md` at the repo root. |
| S29 | `DEPLOY_CHECKLIST.md:310` vs `:385` | `join` answers `"proto":5` vs `"proto":4` | `joined.proto` is `PROTO` = 8 today. |
| S30 | `DEPLOY_CHECKLIST.md:526` | `/version` example `"version":"1.4.0","proto":6` | `SERVER_VERSION = "1.5.0"`, `PROTO = 8` (`app.py:37`, `:1318`). `/version.version` is the **server** version, not the client's `CONFIG.VERSION`. |
| S31 | `DEPLOY_CHECKLIST.md:122-145` | Compose block "is `compose.snippet.yml` verbatim" | The snippet now has `build.args.GIT_SHA` and lacks the commented 1.2.0 env lines. It is not verbatim. |
| S32 | `DEPLOY_CHECKLIST.md:253` | "15 as of this writing" | `race/courses/index.json` has 15 entries today. Still true, but it will drift, so it's dropped. |
| S33 | `race/ACCEPTANCE.md:3-7`, `:21`, `:41-52` | Parts 0–1 use the typed Room box, the lobby card and Manual-sync lead time | Superseded by the shell (its own banner says so). In the merge these rows are kept and marked *rollback UI only*. |
| S34 | `race/ACCEPTANCE.md:39` | `__finsRace.version` is `1.0.0` | It's whatever you're releasing: `1.7.0` today. |
| S35 | `race/ACCEPTANCE.md:450` | "Rolling start / Boost rewrite (1.5.0 …)" | It shipped as **1.7.0** (CHANGELOG, `race.js:12`). |
| S36 | `race/docs/ACCEPTANCE.md:83` | Teleport line expects `resetFlight -> grid slot …` or `llaLocation/htr` | `resetFlight` was removed in 1.7.0. Teleports use `geofs.aircraft.instance.place()` (`GeoPhysics.placeAircraft`). The exact overlay string isn't verified, so the merged row just says "the teleport method line". |
| S37 | `race/CHANGELOG.md:96` | "Unreleased: lobby reliability pass" sits below 1.5.0 | In git it landed before 1.5.0 (`fbec532` precedes `60104a7`). It's re-dated in the normalized changelog, content unchanged. |
| S38 | `race/CHANGELOG.md` | Starts at 1.4.0 | Earlier versions (0.1–1.3.1) are only described in README prose. No history is invented, and the gap is noted in the changelog. |
| S39 | `race/tools/probe.js:970` | Binds **Alt+L** for its landing sampler | This collides with race.js's Alt+L (racing line) if both are loaded. Noted in REFERENCE and here. |
| S40 | `README.md:27`, `race/README.md:72` | COMBINED loader "not yet live-tested" | Can't be verified from the repo. The claim is kept as is. |

### Broken links at the baseline

The relative-link check (`[text](path#anchor)` and `href`/`src`, outside code fences) found
**0 broken links** at `844b229`. The text-only path `race/CLAUDE.md` (S28) is the one stale path
reference.

### Things the brief names that don't exist in the repo

- **`race/server/prune.sh` and `redeploy.sh --no-prune`** don't exist at the baseline.
  `redeploy.sh` accepts only `--dry-run` and `--allow-empty-db`. Backups (`race.db.bak-<ts>`)
  are never pruned by any script in the repo. The runbook documents what exists and marks
  prune *(unverified — not in repo at 844b229)*.
- **"Worktrees", "Claude Code sessions can't push from the PC → push from VS Code terminal"** —
  nothing in the repo says this. The runbook gives only the verifiable part (CLAUDE.md's rules)
  and marks the rest *(unverified)*.
- **"race-autodeploy picks it up within ~5 min"** — verified (`DEPLOY_CHECKLIST.md` §8, the
  User Scripts job named `race-autodeploy` on `*/5 * * * *`).
- **The real Caddyfile path gotcha** — the repo has host path `/mnt/user/appdata/stack/Caddyfile`
  and in-container `/etc/caddy/Caddyfile` (`DEPLOY_CHECKLIST.md` §4). Nothing more specific is
  verifiable.
- **`/docs`** — FastAPI's default interactive docs. `app.py:431` builds `FastAPI(...)` without
  `docs_url=None`, so `/docs` and `/openapi.json` are served (not exercised against the live box).
